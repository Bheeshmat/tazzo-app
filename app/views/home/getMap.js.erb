var mapElem = $('#googleMap');
var mapProp = {
  center:new google.maps.LatLng(<%= @coordinate["Latitude"] %>, <%= @coordinate["Longitude"] %>),
  zoom:5,
  mapTypeId:google.maps.MapTypeId.ROADMAP
};
var map = new google.maps.Map(document.getElementById("googleMap"),mapProp);
$('#myModal').modal('show')
